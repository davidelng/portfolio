---
interface Props {
    href: string;
    title: string;
}

const { href, title } = Astro.props;
---

<a href={href} target="_blank" rel="noreferrer" class="font-bold hover:text-neutral-100">{title}</a>